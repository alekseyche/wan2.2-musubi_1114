# 激活你的环境 (如果你还没激活)

conda create -n musubi_env python=3.10 -y

# 2. 激活新环境
conda activate musubi_env

# 安装 PyTorch 及其核心依赖，使用 CUDA 12.1 源
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

python
>>> import torch
>>> print("PyTorch 版本:", torch.__version__)
>>> print("CUDA 是否可用:", torch.cuda.is_available())
>>> if torch.cuda.is_available():
...     print("PyTorch 检测到的 CUDA 版本:", torch.version.cuda)
...

# 确保在仓库根目录
cd /workspace 

# 1. 将临时大文件添加到 .gitignore 并移除 Git 跟踪
echo "ComfyUI/core.*" >> /workspace/.gitignore
git rm --cached ComfyUI/core.23303 ComfyUI/core.24153

# 2. 确认所有配置（包括 LFS 跟踪的 .gitattributes 和 .gitignore）都已暂存
git add .
